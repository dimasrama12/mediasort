# Plan — Slice 7 Grouping

Spec: `docs/superpowers/specs/2026-09-03-grouping-design.md`. TDD; each sub-slice its own branch,
`--ff-only` merge to `main` when `cargo test` + `npm test -- --run` are green.

## #7a — backend grouping core (branch `slice-7a-grouping-core`)
1. `model.rs`: add `GroupType { Visual, Temporal }` (serde lowercase) + `FileGroup` (serde camelCase)
   + a serialize test.
2. `grouping.rs`: `dhash`, `hamming`, `similarity`, `cluster_visual`, `group_temporal`, `fmt_utc`.
   `#[allow(dead_code)]` until #7b wires commands (mirrors `scan.rs`).
3. `lib.rs`: `mod grouping;`.
4. Tests (all pure / tempdir):
   - `hamming`/`similarity` arithmetic incl. identical (100) and fully-inverted (0).
   - `dhash` on synthetic PNGs: a left-black/right-white gradient vs an exact copy ⇒ distance 0;
     vs its horizontal mirror ⇒ large distance.
   - `cluster_visual`: three near-identical + one far ⇒ one group of three at threshold 80; `min_size`
     drops singletons; group `similarity` is the seed-average.
   - `group_temporal`: `[0, 1800, 3600, 100000]` at `window_hours=1` ⇒ first three cluster, last drops
     (singleton); a within-window pair clusters; `time_span` spans first..last.
   - `fmt_utc(0) == "1970-01-01 00:00"`, `fmt_utc(1609459200) == "2021-01-01 00:00"`.
5. `cargo test` green → commit → `--ff-only` merge to `main`.

## #7b — IPC commands + progress + TS
- `grouping.rs`: `group_visual`/`group_temporal` commands (background hashing + `group-progress`).
- `lib.rs`: register handlers.
- `model.rs`/`types.ts`: `FileGroup`/`GroupType` TS mirror; `commands.ts` wrappers; `events.ts` listener.
- Tests: command arg/serde shape (Rust); wrapper unit tests (vitest, mocked invoke).

## #7c — frontend store slice + indicators
- Store: `groups`, `groupMode`, `applyGroups(groups, mode)` stamping `groupId`, `clearGroups()`.
- Toolbar "Group" control (visual/temporal/clear) calling the commands.
- `FileCard` group badge; Sidebar "Groups" section (list + click-to-focus).
- Vitest: reducer stamps/clears `groupId`; Sidebar renders groups; FileCard shows badge.

## #7d (optional) — EXIF dateTaken
- `scan.rs` `build_file_info`: read EXIF `DateTimeOriginal` (add `kamadak-exif`), fallback mtime.
- Test with a tiny EXIF-bearing JPEG fixture; temporal grouping then prefers capture time.
- Only if it lands cleanly without threatening the build; else leave mtime fallback + note.

## Guardrails
- tempdirs only; no origin push; stop + clean branch + written status if genuinely blocked.
